<div class="panel panel-default">

</div>


<div class="panel-body">
<%= simple_form_for @artist do |f| %>

  <% if @artist.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@artist.errors.count, "error") %> prohibited
        this artist from being saved:
      </h2>
      <ul>
        <% @artist.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.input :name, label: false, input_html: {placeholder: "Name"}%>
  <%= f.input :image_url, label: false, input_html: {placeholder: "images.com/yourpicture.jpg"} %>
  <%= f.input :description, label: false, as: :text, input_html: {placeholder: "Artist Bio"} %>
  <%= f.submit "Submit".html_safe, class: 'btn btn-lg btn-danger'%>


<% end %>
</div>
</div>
