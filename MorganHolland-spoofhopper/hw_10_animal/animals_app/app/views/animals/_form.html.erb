<%= form_for(@animal) do |f| %>
  <% if @animal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@animal.errors.count, "error") %> prohibited this animal from being saved:</h2>

      <ul>
      <% @animal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :species %><br>
    <%= f.text_field :species %>
  </div>
  <div class="field">
    <%= f.label :phylum %><br>
    <%= f.text_field :phylum %>
  </div>
  <div class="field">
    <%= f.label :is_vertebrate %><br>
    <%= f.check_box :is_vertebrate %>
  </div>
  <div class="field">
    <%= f.label :no_of_legs %><br>
    <%= f.number_field :no_of_legs %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
